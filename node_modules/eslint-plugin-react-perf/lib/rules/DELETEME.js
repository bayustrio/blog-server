$0.innerHTML = Array.from($0.options)
  .sort(({ textContent: a }, { textContent: b }) =>
    a.toLowerCase().localeCompare(b.toLowerCase())
  )
  .map(
    ({ value, textContent }) =>
      `<option value=\'${value}\'>${textContent}</option>`
  )
  .join("");
